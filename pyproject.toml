[project]
name = "argus-osint"
version = "0.1.0"
description = "OSINT connectors for finance: the free data Bloomberg and OpenBB don't cover, normalized into records and delta-scanned into findings."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Vito Ciciretti" }]
dependencies = [
    "requests>=2.31",
    "pandas>=2.0",
    "xlrd>=2.0",      # GPR daily index ships as .xls
    "openpyxl>=3.1",  # FCA shorts workbook is .xlsx
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
llm = ["anthropic>=0.40"]  # optional: the [llm] executive-briefing layer (synthesis.py)

[project.scripts]
argus = "argus.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/argus"]

[tool.pytest.ini_options]
testpaths = ["tests"]
